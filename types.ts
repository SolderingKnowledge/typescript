let b: boolean;//let b: boolean= true;
let n: number; //let n: number=5;
let s: string; //let s: string="s";
let a: any; //let a: any='b';

let srr: string[]; //let srr: string[]=['s','b']       | let srr: Array<string> = ["b"]
let nrr: number[]; //let nrr: number[]=[1,2]           | let nrr: Array<number>=[1,3]
let brr: boolean[]; //let brr: boolean[]=[true, false] | let brr: Array<boolean>=[true, false]

let ob: {}; // ob= {name: "name"};


let tuple: [string, number]; //['hello', 4]; in tuples order is important and structure, that is why you can't break it
let myVoid: void = null;//null  let myVoid: void = undefined => undefined :: both works for this
